name: Release
on:
  push

jobs:
  release:
    name: Release
    uses: aquasecurity/trivy/blob/main/.github/workflows/reusable-release.yaml
    with:
      goreleaser_config: goreleaser.yml
      goreleaser_options: '--clean --timeout 90m'
    secrets: inherit
